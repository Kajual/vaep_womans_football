# Core data
pandas>=2.0
numpy>=1.24
pyarrow>=14.0
duckdb>=0.9
tqdm>=4.65
pyyaml>=6.0

# Football-specific
statsbombpy>=1.11
socceraction>=1.5
mplsoccer>=1.2
# socceraction 1.5.x pins pandera<0.18, which imports `overload` from multimethod.
# multimethod 2.0 removed that symbol, so we pin to the 1.x line here.
# Remove this line if/when socceraction upgrades its pandera requirement.
multimethod<2.0

# Modelling
scikit-learn>=1.3
xgboost>=2.0
lightgbm>=4.0
catboost>=1.2
joblib>=1.3

# Calibration / stats
scipy>=1.11

# Interpretability
shap>=0.44

# Visualisation
matplotlib>=3.7
plotly>=5.17

# CLI / config
click>=8.1

# Notebooks (optional, for validation only)
jupyter
ipykernel

# Dev (optional)
pytest>=7.4
ruff>=0.1
